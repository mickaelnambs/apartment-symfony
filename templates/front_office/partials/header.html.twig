<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<a class="navbar-brand" href="{{ path("app_home") }}">
		<b>BNB</b>
	</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path("app_home") }}">
					<span class="fa fa-home"></span>
					Accueil
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path("ad_index") }}">
					<span class="fa fa-hotel"></span>
					Annonces
				</a>
			</li>
		</ul>
		<ul class="navbar-nav ml-auto">
			{% if is_granted('ROLE_USER') %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
						{% if app.user.avatar %}
				 			<img src="{{ asset("uploads/avatar/" ~ app.user.avatar) }}" class="avatar avatar-mini" alt="Avatar de {{app.user.fullName}}"> {{app.user.firstName}}
						{% else %}
							<img src="{{ asset('uploads/img/avatar.png' ) }}" class="avatar avatar-mini" alt="Avatar de {{app.user.firstName}}"> {{app.user.firstName}}
						{% endif %}
					</a>
					<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDopdownLink">
						<a class="dropdown-item" href="{{ path("ad_new")}}">
							Créer une annonce
						</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{ path("my_account")}}">
							Mon compte
						</a>
						<a class="dropdown-item" href="{{ path("profile_update")}}">
							Modifier mon profil
						</a>
						<a class="dropdown-item" href="{{ path("password_update")}}">
							Modifier mon mot de passe
						</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{ path("app_logout") }}">
							Déconnexion
						</a>
					</div>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_register') }}">
						<span class="fa fa-sign-in"></span>
						Inscription
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_login') }}">
						<span class="fa fa-lock"></span>
						Connexion
					</a>
				</li>
			{% endif %}
		</ul>
	</div>
</div></nav>
